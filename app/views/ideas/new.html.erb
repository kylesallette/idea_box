<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title></title>
  </head>
  <body>
<div class="form">
    <div class="wrapper">
      <%= form_for [@user, @idea] do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name, placeholder: "Idea Title*" %><br>

        <%= f.label :content %>
        <%= f.text_area :content, placeholder: "Idea*" %><br>
        <%= f.collection_select(:categories_id, Category.all, :id, :title) %>
          <div class="btn btn-info">
  <%= f.submit class: "sub-btn" %>
</div>
<% end %>
</body>
</html>
