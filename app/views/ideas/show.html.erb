<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title></title>
  </head>
  <body>
<h3><%= @idea.name %></h3>
<%= @idea.content %><br>
<%= image_tag(@idea.images[0].url, size: "150x150") %>
  <div class="btn btn-info">
<%= button_to "Back to ideas", user_ideas_path(@user), method: "get" %><br/>
</div>
</body>
</html>
