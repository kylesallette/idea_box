<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/animate.css@3.5.2/animate.min.css">
  </head>
  <body>
    <div id="left"></div>
    <div id="right"></div>
    <div id="top"></div>
    <div id="bottom"></div>
    <header>
      <img class="animated flipInY"src="https://i.imgur.com/tLVDqbW.jpg" alt="banner" height="300px" width="100%">
    </header>
    <div class="login">
      <h2> Welcome, <%= @user.name.capitalize %>!</h2><br/>
      <%= link_to "Add  Category", new_admin_category_path if current_user.admin? %>
       <br>
      <%= link_to "Add  Image", new_admin_image_path(current_user) if current_user.admin? %>
      <%= button_to "Add a New Idea", new_user_idea_path(@user), method: "get"%><br/>
      <%= button_to "View all ideas", user_ideas_path(@user), method: "get" %><br/>
      <span class="button-wrap">
        <div class="button button-raised button-primary button-pill">
          <%= link_to "Log Out", logout_path, method: :delete %>
        </div>
      </span>
    </div>
</html>
