<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <header>
  <img class=""src="https://i.imgur.com/w4o2Kqt.jpg" alt="banner" height="300px" width="100%">
</header>
    <body>
      <div class="login-2">
        <h3>Sign up here!</h3>
          <div class="wrapper">
            <%= form_for @user do |f| %>
            <%= f.label :Username %>
            <%= f.text_field :name, placeholder: "new user name.*" %>
            <br>
            <%= f.label :Email %>
            <%= f.text_field :email, placeholder: "please enter your email.*" %>
            <br>
            <%= f.label :password %>
            <%= f.password_field :password, placeholder: "Choose a Password.*" %><br>
            <br>
            <p>* required field</p>
              <div class="btn btn-info">
              <%= f.submit "Create User", class: "sub-btn" %>
            </div>
              <% end %>
            </div>
          </div>
        </body>
    </html>
