<!DOCTYPE html>
  <html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <header>
    <img class=""src="https://i.imgur.com/PHJtGpv.jpg" alt="banner" height="300px" width="100%">
  </header>
  <body>
    <div id="left"></div>
    <div id="right"></div>
    <div id="top"></div>
    <div id="bottom"></div>
    <div class="login-5">
      <div class="wrapper">
        <%= form_for [:admin, @category] do |f| %>
        <%= f.label :title %>
        <%= f.text_field :title, placeholder: "new category*" %><br>
        <%= f.submit class: "sub-btn" %>
      </div>
      <% end %>
      <br>
      <div class="wrapper">
        <%= form_for [:admin, @image] do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name, placeholder: "name for image*" %><br>
        <%= f.label :url %>
        <%= f.text_field :url, placeholder: "Image url*" %><br>
        <%= f.submit class: "sub-btn" %>
      </div>
      <% end %>
    </body>
</html>
