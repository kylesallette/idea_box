<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/animate.css@3.5.2/animate.min.css">
  </head>
  <header>
    <img class="animated flipInY"src="https://i.imgur.com/O4V862J.jpg" alt="banner" height="300px" width="100%">
  </header>
  <body>
    <div class="btn btn-info">
      <%= button_to "Home", user_path(current_user), method: "get" %><br/>
    </div>
    <div id="left"></div>
    <div id="right"></div>
    <div id="top"></div>
    <div id="bottom"></div>

        <% Image.all.each do |image| %>

        <%= image_tag(image.url, :class=> "img-circular")%>
        <%= check_box_tag("image_url[]",image.id) %>
        <span class="button-wrap">
          <div class="button button-raised button-primary button-pill">
            <%= link_to "Delete", admin_image_path(image), method: "delete" %>
          </div>
        </span>
        <% end %>
  </div>
</body>
</html>
